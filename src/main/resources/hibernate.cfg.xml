<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC "-//Hibernate/Hibernate Configuration DTD 3.0//EN" "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Connection settings will be set via Config.java -->

        <!-- JDBC connection pool (use the built-in) -->
        <property name="connection.pool_size">1</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.PostgreSQLDialect</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">true</property>

        <!-- Auto-generation of tables (update to avoid data loss, or validate) -->
        <property name="hbm2ddl.auto">update</property>

        <!-- Mapping classes -->
        <mapping class="org.example.Model.BooksModel" />
        <mapping class="org.example.Model.BorrowItemsModel" />
        <mapping class="org.example.Model.BorrowSlipsModel" />
        <mapping class="org.example.Model.FinesModel" />
        <mapping class="org.example.Model.MemberModel" />
        <mapping class="org.example.Model.UserModel" />
    </session-factory>
</hibernate-configuration>